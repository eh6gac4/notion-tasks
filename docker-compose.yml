services:
  dev:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
      - pkg_cache:/cache
    env_file:
      - .env.local

volumes:
  node_modules:
  pkg_cache:
